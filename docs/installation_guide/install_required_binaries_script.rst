.. _install_required_binaries_script:

install_required_binaries.sh
============================

This shell script, located in the ``/src`` subdirectory of the elaspic root folder, downloads and 
compiles all programs that are not mentioned explicitly in the :ref:`install_externals` section.


-------------------------------------------------------------------------------------------------

Source code
-----------

.. literalinclude:: ../../src/install_required_binaries.sh
   :language: bash

